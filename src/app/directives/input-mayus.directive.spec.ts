import { InputMayusDirective } from './input-mayus.directive';

describe('InputMayusDirective', () => {
  it('should create an instance', () => {
    const directive = new InputMayusDirective();
    expect(directive).toBeTruthy();
  });
});
